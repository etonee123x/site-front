export const ICON = Object.freeze({
  default: 'icon',
  SIZE: Object.freeze({
    SM: 'icon_size_sm',
  }),
});

export const BUTTON = Object.freeze({
  default: 'button',
});

export const VR = Object.freeze({
  default: 'vr',
});
